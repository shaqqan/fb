export enum RoleEnum {
  MODER = 'MODER',
  ADMIN = 'ADMIN',
}

export enum NewsStatus {
  DRAFT = 'DRAFT',
  PUBLISHED = 'PUBLISHED',
  ARCHIVED = 'ARCHIVED',
  DELETED = 'DELETED',
  PENDING = 'PENDING',
}

export enum StaffStatus {
  ACTIVE = 'ACTIVE',
  NOACTIVE = 'NOACTIVE',
}

export enum PartnerStatus {
  ACTIVE = 'ACTIVE',
  NOACTIVE = 'NOACTIVE',
}